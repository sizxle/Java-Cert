package study.cert.types;

public class Primitive {
    /**
     * we have 8 primitive types
     * :byte(1), short(2), int(4), long(8), float(4), double(8) , char(2), boolean(1)
     *
     *  whole number are seen as integers by default
     *  floating point numbers are seen as a double by default
     */

    char letter= 'T';
}
